<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="MemberDAO">

	<select id="getMemberInfo" resultType="memberInfo">
		<![CDATA[
			SELECT * FROM memberinfo WHERE id = #{id} AND pw = #{pw} 
		]]>
	</select>

	<select id="getUserIssuance" resultType="userIssuance">
		<![CDATA[
			SELECT * 
			FROM issuance 
			INNER JOIN cardinfo 
			ON issuance.cardName = cardinfo.name 
			WHERE userId = #{id}
		]]>		
	</select>
	
	<select id="countOfIssuanceByMember" resultType="int">
		<![CDATA[
			SELECT COUNT(*) 
			FROM issuance
			WHERE issCondition = 2 AND userId = #{id}
		]]>
	</select>
</mapper>
